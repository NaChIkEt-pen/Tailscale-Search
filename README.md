# Tailscale-Search

## Build from Source
1. Rust should already be installed.
2. Clone the repo.
3. Add usernames in the main.rs
4. Run ```cargo build --release```
5. Run ```sudo cp target/release/ts-search /usr/local/bin/ts-search```